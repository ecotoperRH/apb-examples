- name: Deprovision NGINX OSS from OpenShift/Kubernetes
  hosts: localhost
  gather_facts: false
  connection: local
  tasks:
  - name: Run deprovision tasks
    ansible.builtin.include_tasks: '{{ playbook_dir }}/../tasks/deprovision.yml'
