# test-85b0f9

Ansible project generated by x2a-convertor.

## Roles


### apb_base


APB base image runtime — modernized scripts, Ansible config, and EE build artifacts


**Target platforms:** Ubuntu, EL


Run this role:

```bash
ansible-playbook run_apb_base.yml -i inventory/hosts.yml
```


**Default variables** (override in inventory or extra vars):

| Variable | Default |
|----------|---------|

| `apb_user_name` | `apb` |

| `apb_user_uid` | `1001` |

| `apb_base_dir` | `/opt/apb` |

| `apb_roles_path` | `/etc/ansible/roles:/opt/ansible/roles` |

| `apb_collections_path` | `/usr/share/ansible/collections:/opt/ansible/collections` |

| `apb_secrets_dir` | `/etc/apb-secrets` |

| `apb_bind_creds_path` | `/var/tmp/bind-creds` |

| `apb_test_result_path` | `/var/tmp/test-result` |

| `apb_bind_retries` | `150` |

| `apb_bind_sleep` | `2` |

| `openshift_target` | `https://kubernetes.default` |

| `openshift_token` | `` |




## Prerequisites

### Collections

Install required Ansible collections:

```bash
ansible-galaxy collection install -r collections/requirements.yml
```


## Inventory

Edit `inventory/hosts.yml` to define your target hosts before running any playbook.

## Quick start

```bash
# Install required collections
ansible-galaxy collection install -r collections/requirements.yml

# Edit inventory
vi inventory/hosts.yml

# Run a playbook
ansible-playbook run_<role_name>.yml -i inventory/hosts.yml
```